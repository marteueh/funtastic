[build]
builder = "nixpacks"

[deploy]
startCommand = "cd /app && php server.php"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Railway rileva automaticamente PHP e installa le dipendenze
